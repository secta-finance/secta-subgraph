{
  "name": "blocks",
  "description": "SectaFi Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:secta-finance/secta-subgraph.git",
  "author": "SectaFi",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen subgraph.yaml",
    "build": "graph build subgraph.yaml",
    "deploy": "graph deploy --node https://graph-deploy.goerli.linea.build --ipfs https://graph-ipfs.goerli.linea.build sectafi/blocks-goerli subgraph.yaml"
  }
}
