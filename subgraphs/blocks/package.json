{
  "name": "blocks",
  "description": "SectaFi Subgraph",
  "version": "1.0.0",
  "repository": "git@github.com:secta-finance/secta-subgraph.git",
  "author": "SectaFi",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen:testnet": "graph codegen subgraph.yaml",
    "codegen:mainnet": "graph codegen sub_main.yaml",
    "build:testnet": "graph build subgraph.yaml",
    "build:mainnet": "graph build sub_main.yaml",
    "deploy:testnet": "graph deploy --node https://graph-deploy.goerli.linea.build --ipfs https://graph-ipfs.goerli.linea.build sectafi/blocks-goerli subgraph.yaml",
    "deploy:mainnet": "graph deploy --node https://graph-deploy.linea.build --ipfs https://graph-ipfs.linea.build sectafi/blocks sub_main.yaml",
    "deploy:linea": "graph deploy --studio secta-linea-blocks sub_main.yaml"
  }
}
